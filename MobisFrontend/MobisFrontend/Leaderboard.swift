import SwiftUI

struct Leaderboard: View {
    var body: some View {
        Text("Welcome to the dab!")
            .padding()
    }
}

struct Leaderboard_Previews: PreviewProvider {
    static var previews: some View {
        Leaderboard()
    }
}